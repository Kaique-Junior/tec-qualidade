/**
 * Página do ToDo List - Versão inicial simples.
 * 
 * Por enquanto contém apenas um título básico.
 * Será expandida com funcionalidades completas posteriormente.
 */
export default function TodoPage() {
  return (
    <div className="min-h-screen bg-[#0b0f19] flex items-center justify-center">
      <h1 className="text-3xl font-bold text-white">Página do ToDo List</h1>
    </div>
  );
}